import { FilmDurationValidatorDirective } from './film-duration-validator.directive';

describe('FilmDurationValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new FilmDurationValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
